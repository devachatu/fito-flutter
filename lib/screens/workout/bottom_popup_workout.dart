import 'package:flutter/material.dart';
import 'package:flutter/cupertino.dart';

class BottomPopupWorkout extends StatefulWidget {
  const BottomPopupWorkout({Key? key}) : super(key: key);

  @override
  State<BottomPopupWorkout> createState() => _BottomPopupWorkoutState();
}

class _BottomPopupWorkoutState extends State<BottomPopupWorkout> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
